/**: # wildcard for the node name
  ros__parameters:
    qtm_body_names: ['UVMS']
    ros_body_names: ['klopsi00']
    server_address: '192.168.0.166'
    # if true, the node needs to be restarted after mocap timoeut to publish again
    latch_timeout: [false]
    # if true, we publish the px4/FCU visual odometry topic
    publish_visual_odometry: [true]
