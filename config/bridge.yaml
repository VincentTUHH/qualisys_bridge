/**: # wildcard for the node name
  ros__parameters:
    #qtm_body_names: ['uvms', 'platform', 'cylinder', 'cylinder_holder']
    #ros_body_names: ['klopsi00', 'platform', 'cylinder', 'cylinder_holder']
    qtm_body_names: ['uvms', 'christmasStar']
    ros_body_names: ['klopsi00', 'star']
    server_address: '192.168.0.181'
    # if true, the node needs to be restarted after mocap timoeut to publish again
    # latch_timeout: [false, false, false, false]
    latch_timeout: [false, false]
    # if true, we publish the px4/FCU visual odometry topic
    #publish_visual_odometry: [true, true, true, true]
    publish_visual_odometry: [true, true]
